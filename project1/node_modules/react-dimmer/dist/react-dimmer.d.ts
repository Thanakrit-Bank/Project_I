/// <reference types="react" />
import './react-dimmer.css';
export interface IDimmerProps {
    isOpen: boolean;
    exitDimmer: (action: boolean) => void;
    zIndex?: number;
    opacity?: number;
    blur?: number;
    saturate?: number;
    transition?: number;
}
export declare const ReactDimmer: ({ isOpen, exitDimmer, zIndex, opacity, blur, saturate, transition }: IDimmerProps) => JSX.Element;
