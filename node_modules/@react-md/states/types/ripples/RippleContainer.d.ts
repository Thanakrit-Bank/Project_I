import type { ReactElement } from "react";
import type { CSSTransitionClassNames, TransitionTimeout } from "@react-md/transition";
import type { RipplesState, RippleState } from "./types";
export interface RippleContainerProps {
    ripples: RipplesState;
    entered: (ripple: RippleState) => void;
    exited: (ripple: RippleState) => void;
    className?: string;
    rippleClassName?: string;
    timeout?: TransitionTimeout;
    classNames?: CSSTransitionClassNames;
}
export declare function RippleContainer({ ripples, className, rippleClassName, timeout, classNames, entered, exited, }: RippleContainerProps): ReactElement;
